name: Manually triggered workflow

on:
  pull_request:
    branches: 
      - master
 
jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo "Hello ${{ github.event.GITHUB_REF }}!"
     
