name: Manually triggered workflow

on:
  pull_request:
    branches: 
      - master
 
jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
    - run: |
         echo "if: github.event_name == 'pull_request' && contains(github.head_ref, 'my-feature-branch')"
         echo "Hello ${{ github.event.GITHUB_REF }}!"
     
