name: Shell commands
on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello puneet how are you!"
      - name: multilnie script
        run: |
          node -v
          npm -v
        
        
    
     
